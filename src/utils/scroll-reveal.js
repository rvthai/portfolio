import ScrollReveal from "scrollreveal"

const sr = ScrollReveal()
sr.config = {
  origin: "top",
  duration: 1000,
  distance: "50px",
  scale: 1,
  easing: "ease",
  reset: false,
}

export default sr
